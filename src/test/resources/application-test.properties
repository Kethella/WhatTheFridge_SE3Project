# the properties below are intended for integration tests
# they are merged with and take precedence over src/main/resources/application.properties
# pre-requisite: a running mongodb instance reachable under "mongo"
# can be either provisioned manually or in a CI/CD environment automatically
spring.data.mongodb.uri=mongodb://root:dummy@mongo
spring.data.mongodb.authentication-database=admin
spring.data.mongodb.auto-index-creation=true

